#include<iostream>
#include<stdlib.h>
#include <windows.h>
using namespace std;

void pointers_references(void);
int main(void)
{
    SetConsoleOutputCP(65001);
    pointers_references();
    return 0;
}
